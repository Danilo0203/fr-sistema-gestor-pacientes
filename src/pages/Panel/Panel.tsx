import { useEffect } from "react";
import { useUsuarioStore } from "../../store/usuarios";

export const Panel = () => {
  // const usuarios = useUsuarioStore((state) => state.execute);
  // const dataUsuarios = useUsuarioStore((state) => state.data);
  // useEffect(() => {
  //   usuarios();
  // }, [usuarios]);
  // console.log(dataUsuarios);

  return <div>Hola</div>;
};
